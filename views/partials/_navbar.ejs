<nav class="navbar fade-in premium-navbar" role="navigation" aria-label="Main navigation">
  <div class="navbar-container">
    <!-- Logo Section -->
    <a href="/" class="navbar-logo" aria-label="Home">
      <div class="navbar-logo-icon">
        <i class="fas fa-cake-candles"></i>
      </div>
      <span class="navbar-logo-text">Sweet Delights</span>
    </a>

    
    <button class="navbar-toggle" aria-label="Toggle navigation" aria-expanded="false" aria-controls="navbar-links">
      <i class="fas fa-bars"></i>
    </button>

   
    <div class="navbar-links" id="navbar-links">
     
      <a href="/" class="nav-link <%= (typeof active !== 'undefined' && active === 'home') ? 'active' : '' %>" aria-label="Home">
        <i class="fas fa-home"></i>
        <span>Home</span>
      </a>
      
      <a href="/products" class="nav-link <%= (typeof active !== 'undefined' && active === 'products') ? 'active' : '' %>" aria-label="Products">
        <i class="fas fa-birthday-cake"></i>
        <span>Products</span>
      </a>
      
      <a href="/about" class="nav-link <%= (typeof active !== 'undefined' && active === 'about') ? 'active' : '' %>" aria-label="About Us">
        <i class="fas fa-heart"></i>
        <span>About Us</span>
      </a>

      
      <% if (user) { %>
        <% if (user.role === 'admin') { %>
          
          <div class="nav-dropdown">
            <button class="nav-dropdown-toggle" aria-label="Admin Menu">
              <i class="fas fa-crown"></i>
              <span>Admin</span>
              <i class="fas fa-chevron-down"></i>
            </button>
            <div class="nav-dropdown-menu">
              <a href="/admin/dashboard" class="<%= (typeof active !== 'undefined' && active === 'admin-dashboard') ? 'active' : '' %>" aria-label="Dashboard">
                <i class="fas fa-tachometer-alt"></i>
                <span>Dashboard</span>
              </a>
              <a href="/admin/products" class="<%= (typeof active !== 'undefined' && active === 'admin-products') ? 'active' : '' %>" aria-label="Admin Products">
                <i class="fas fa-box"></i>
                <span>Products</span>
              </a>
              <a href="/admin/orders" class="<%= (typeof active !== 'undefined' && active === 'admin-orders') ? 'active' : '' %>" aria-label="Orders">
                <i class="fas fa-clipboard-list"></i>
                <span>Orders</span>
              </a>
              <a href="/admin/users" class="<%= (typeof active !== 'undefined' && active === 'admin-users') ? 'active' : '' %>" aria-label="Users">
                <i class="fas fa-users-cog"></i>
                <span>Users</span>
              </a>
              <a href="/admin/analytics" class="<%= (typeof active !== 'undefined' && active === 'admin-analytics') ? 'active' : '' %>" aria-label="Analytics">
                <i class="fas fa-chart-bar"></i>
                <span>Analytics</span>
              </a>
            </div>
          </div>
        <% } else { %>
          
          <div class="nav-dropdown">
            <button class="nav-dropdown-toggle" aria-label="Account Menu">
              <div class="user-avatar">
                <img src="<%= user.avatar || '/images/default-avatar.svg' %>" alt="<%= user.name %>" id="navbarUserAvatar">
              </div>
              <span><%= user.name %></span>
              <i class="fas fa-chevron-down"></i>
            </button>
            <div class="nav-dropdown-menu">
              <a href="/auth/profile" class="<%= (typeof active !== 'undefined' && active === 'profile') ? 'active' : '' %>" aria-label="My Profile">
                <i class="fas fa-user"></i>
                <span>My Profile</span>
              </a>
              <a href="#" onclick="openSettingsModal()" class="settings-link" aria-label="Settings">
                <i class="fas fa-cog"></i>
                <span>Settings</span>
              </a>
              <a href="/orders" class="<%= (typeof active !== 'undefined' && active === 'orders') ? 'active' : '' %>" aria-label="My Orders">
                <i class="fas fa-shopping-bag"></i>
                <span>My Orders</span>
              </a>
              <a href="/wishlist/page" class="<%= (typeof active !== 'undefined' && active === 'wishlist') ? 'active' : '' %>" aria-label="Wishlist">
                <i class="fas fa-heart"></i>
                <span>Wishlist</span>
              </a>
              <div class="dropdown-divider"></div>
              <a href="/auth/logout" class="logout-link" aria-label="Logout">
                <i class="fas fa-sign-out-alt"></i>
                <span>Logout</span>
              </a>
            </div>
          </div>
        <% } %>
      <% } else { %>
        
        <a href="/auth/login" class="nav-link <%= (typeof active !== 'undefined' && active === 'login') ? 'active' : '' %>" aria-label="Login">
          <i class="fas fa-sign-in-alt"></i>
          <span>Login</span>
        </a>
      <% } %>

      
      <a href="/cart" class="navbar-cart" aria-label="Shopping Cart">
        <i class="fas fa-shopping-bag"></i>
        <span class="cart-text">Cart</span>
        <span class="cart-count">(<%= cartCount || 0 %>)</span>
      </a>
    </div>
  </div>
</nav>


<style>
  .navbar-logo-icon {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, var(--accent), var(--secondary));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.2rem;
    box-shadow: var(--shadow-soft);
  }

  .navbar-logo-text {
    font-family: 'Playfair Display', serif;
    font-weight: 800;
    background: linear-gradient(135deg, var(--accent), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .nav-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.2rem;
    border-radius: var(--radius-pill);
    transition: all var(--transition);
    position: relative;
    overflow: hidden;
    text-decoration: none;
    color: var(--text);
    font-weight: 500;
  }

  .nav-link i {
    font-size: 1rem;
    transition: transform var(--transition);
  }

  .nav-link:hover i {
    transform: scale(1.1);
  }

  .nav-link.active {
    background: var(--accent-bg);
    color: var(--accent-dark);
    font-weight: 600;
  }

  .logout-link {
    color: #e74c3c !important;
  }

  .logout-link:hover {
    background: rgba(231, 76, 60, 0.1) !important;
    color: #c0392b !important;
  }

  .cart-text {
    display: inline;
  }

  .cart-count {
    font-weight: 700;
    color: white;
  }

  
  .nav-dropdown {
    position: relative;
  }

  .nav-dropdown-toggle {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.2rem;
    border-radius: var(--radius-pill);
    transition: all var(--transition);
    background: none;
    border: none;
    color: var(--text);
    font-weight: 500;
    cursor: pointer;
    font-size: 1rem;
  }

  .user-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    overflow: hidden;
    border: 2px solid var(--accent-light);
    transition: all var(--transition);
  }

  .user-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition);
  }

  .nav-dropdown-toggle:hover .user-avatar {
    border-color: var(--accent);
    transform: scale(1.05);
  }

  .nav-dropdown-toggle:hover .user-avatar img {
    transform: scale(1.1);
  }

  .nav-dropdown-toggle:hover {
    background: var(--accent-bg);
    color: var(--accent-dark);
  }

  .nav-dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(20px);
    border-radius: var(--radius);
    box-shadow: var(--shadow-lg);
    padding: 1rem 0;
    min-width: 200px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all var(--transition);
    border: 1px solid rgba(255, 255, 255, 0.3);
    z-index: 1000;
  }

  .nav-dropdown:hover .nav-dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .nav-dropdown-menu a {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.5rem;
    color: var(--text);
    text-decoration: none;
    transition: all var(--transition);
    font-weight: 500;
  }

  .nav-dropdown-menu a:hover {
    background: var(--accent-bg);
    color: var(--accent-dark);
  }

  .nav-dropdown-menu a.active {
    background: var(--accent-bg);
    color: var(--accent-dark);
    font-weight: 600;
  }

  .dropdown-divider {
    height: 1px;
    background: var(--border);
    margin: 0.5rem 0;
  }

  .nav-dropdown-menu .logout-link {
    color: #e74c3c !important;
  }

  .nav-dropdown-menu .logout-link:hover {
    background: rgba(231, 76, 60, 0.1) !important;
    color: #c0392b !important;
  }

  .settings-link {
    color: #4361EE !important;
  }

  .settings-link:hover {
    background: rgba(67, 97, 238, 0.1) !important;
    color: #3a0ca3 !important;
  }

  
  @media (max-width: 768px) {
    .navbar-links {
      position: fixed;
      top: 100%;
      left: 0;
      width: 100%;
      background: rgba(255, 255, 255, 0.98);
      backdrop-filter: blur(20px);
      flex-direction: column;
      padding: 2rem;
      transform: translateY(-100%);
      opacity: 0;
      visibility: hidden;
      transition: all var(--transition);
      box-shadow: var(--shadow-lg);
      border-top: 1px solid rgba(255, 255, 255, 0.3);
    }

    .navbar-links.open {
      transform: translateY(0);
      opacity: 1;
      visibility: visible;
    }

    .navbar-toggle {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      height: 44px;
      border-radius: var(--radius-small);
      transition: all var(--transition);
    }

    .navbar-toggle:hover {
      background: var(--accent-bg);
      color: var(--accent-dark);
    }

    .nav-dropdown-menu {
      position: static;
      opacity: 1;
      visibility: visible;
      transform: none;
      box-shadow: none;
      background: transparent;
      border: none;
      padding: 0 0 0 2rem;
      margin-top: 0.5rem;
    }

    .cart-text {
      display: none;
    }

    .navbar-cart {
      justify-content: center;
      min-width: 60px;
    }
  }

  @media (max-width: 480px) {
    .navbar-logo-text {
      display: none;
    }

    .navbar-logo {
      gap: 0;
    }
  }
</style>

  